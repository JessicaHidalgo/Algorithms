//Partiendo del primer número se compara con todos los demás y se selecciona el número menor 
//toda la cadena comparado con el primero.
const arrayselection = [0,2,3,4,5,6,7,8];
let temp
if (arrayselection(0)<arrayselection(i)){
    arrayselection(i)
    
}

